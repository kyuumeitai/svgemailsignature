html
  head
    title= nombre
  body
    table(cellpadding='0', cellspacing='0', width='auto')
      tr
        td
          a(href=url, target='_blank')
            img(src='/logo-firma.gif', width='160' height='80')
        td
          a(href=url, target='_blank')
            img(src=`${permalink}/${slug}-alt.png`, width='100%' style=`display: block; max-width: ${Math.round(size.width / 2)}px`)
